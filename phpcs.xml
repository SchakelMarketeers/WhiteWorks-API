<?xml version="1.0"?>
<ruleset>
  <!-- Directories -->
  <file>src</file>
  <file>tests</file>

  <!-- Ignore some directories -->
  <exclude-pattern>vendor/</exclude-pattern>

  <!-- We just use PSR-2 -->
  <rule ref="PSR2"/>

  <!-- Configs -->
  <arg name="colors"/>

</ruleset>
